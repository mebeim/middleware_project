{% if not headok|default(false) -%}
<?xml version="1.0" encoding="UTF-8"?>
{% set headok = true -%}
{% endif -%}

<images owner="{{owner_id}}">
{%- for id, title, _ in images %}
{% include 'image.xml' %}
{%- endfor %}
</images>
